# Workflow Feedback — <Unit> / <Verb> / <Mode>

<!-- Copy this file to <YYMMDD>_<Unit>_<Verb>_<Mode>.md. Fill it from YOUR run context.
     Delete every angle-bracket placeholder and every one of these comments as you go.
     Full instructions: .ai/commands/retro.md -->

| | |
|---|---|
| **Verb** | `<research \| discovery \| plan \| execute \| quick-fix>` |
| **Unit** | `<phase (P01) \| Issue #<n> \| research slug>` |
| **Mode** | `<cold start \| resume run N \| suspend-handoff \| close \| from-issue>` |
| **Outcome** | `<done \| derailed \| suspended \| repaired \| promoted \| n/a>` — one clause on what actually landed |
| **Shape** | `<behavior-touching \| doc-only>` |
| **Confidence** | Ran fully: `<verbs you actually exercised>`. **Not exercised:** `<the rest>`. Items below touching those are inferential and flagged. |
| **Date** | `<YYYY-MM-DD>` |

<!-- Second or later deposit from ONE long run? Say so here and name the file it continues,
     and read _RefinementBacklog.md first so you don't re-file an item already absorbed. -->

---

## Items, ranked

<!-- Highest-value first. Ground EVERY item in this run's friction — the Refiner declines
     generic best-practice advice. In a freshly-adopted project the most common item is a
     PORT GAP: a carried command still assuming the upstream's shape. Flag it [doc-gap] and
     point "Where it'd live" at LOCAL_DELTAS.md. -->

### 1 — <the claim, in one line> `[doc-gap]` / `[self-error-doc-could-prevent]`

**Grounding.** What actually happened this run — the repro, not an abstraction. Name the
file, the line, the command, the thing that broke or the wrong turn you took.

**Proposed fix.** Concrete. Ideally the sentence you'd want to read.

**Where it'd live.** `<execute.md | research.md | Workflow | a docs/Learnings/ entry | memory | LOCAL_DELTAS.md (port gap) | unsure>`

---

### 2 — …

---

## Keep these

<!-- What worked and must NOT be "fixed". This is load-bearing: it is the only signal that
     stops the Refiner pruning a rule that earned its place. One line each, and say what it
     bought you this run. -->

- **<rule / discipline>** — what it caught or prevented, concretely.

## Dead weight

<!-- The inverse signal — what to CUT. Rules you ignored, didn't need, or had to re-read
     because they were buried. Flags accrue across runs in _RefinementBacklog.md; a
     candidate that keeps getting flagged graduates to a cut.
     "None" is a fine and common answer — do not fabricate. -->

None.

## Open questions

<!-- Anything you could not resolve, or that needs a LEAD decision rather than a Refiner
     tweak. Say which it is. -->

1. …
