# Phase NN–MM — <Name> Wave

**Status:** SEED.

> **Before using this:** read `AgenticEngineering_Workflow.md` §What counts as ONE Wave and
> run its three tests. A Wave is **coupling, not size** — several Phases that must stay
> consistent with each other. If the parts only make sense delivered together they are
> **Steps** in one Phase; if they merely share a subject they are independent phases the
> Roadmap already sequences. Neither needs this doc.
>
> **Keep it thin.** This doc is authoritative for what the members SHARE and nothing else.
> Anything one member owns lives in that member's phase doc — a Wave doc that restates
> member detail becomes the second contradicting source it exists to prevent. Sections you
> have no answer for stay empty; delete any that never apply to this Wave.

## Goal

<What the Wave delivers as a whole, in plain language. Not a summary of the members — the
thing that is only true once all of them have landed.>

## Why this is a Wave

<Name the coupling. The test is: if these phases each went their own way, what would break?
A shared contract, a required order, a gate none of them closes alone. If this section is
hard to write, the answer is probably "not a Wave".>

## Members

<The phases this Wave coordinates, in order. Keep each line to identity + one-line outcome
+ a pointer — the detail lives in the phase doc. Members stay `PhaseTBD_` until the lead
numbers them — and committing to a Wave is one of the normal moments to number the whole
span at once, since the order is usually what the Wave just settled (§Phase Numbering).>

| Phase | Outcome (what the user can DO) | Doc | Status |
|---|---|---|---|
| | | | |

## Shared contracts

<The surfaces more than one member touches, and **who owns each**. This is the section the
Wave exists for, and the one siblings read before designing anything — a surface a member
deliberately scoped OUT is recorded here too, because that is often exactly what the next
member is about to re-open.>

| Surface | Owner | Shape / decision | Notes |
|---|---|---|---|
| | | | |

## Order and dependencies

<What must precede what, and why. Only real constraints — an order that is merely
convenient is not a dependency, and recording it as one calcifies the sequence.>

## Exit gate

<What must be true for the WAVE to be done, beyond every member being closed. If the honest
answer is "all members closed", say exactly that and keep the section — an empty gate is a
finding, not an omission.>

## Open decisions

<Wave-level forks only — ones that bind more than one member. A decision inside one member
belongs in that member's doc.>

## Notes

<Provenance worth keeping: where this Wave came from (recognised in research, carved out of
a phase whose discovery outgrew it, split from work already in flight), and any origin doc
it draws on. If it was carved from an existing phase doc, say where that doc's findings
went — every finding needs an owner.>

## Wave Status

- **Members closed:** —
- **Current state:** —
- **Open decisions:** —
- **Checks to carry forward:** —

_(Rewrite "current state" each time — replace it with what is true now. The story of how it
changed lives in the member docs' passes; a field that is appended to becomes a narrative.)_
